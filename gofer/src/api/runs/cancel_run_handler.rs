use crate::api::Api;

impl Api {
    // pub async fn cancel_run(&self, namespace_id: &str, pipeline_id: &str, run_id: u64) ->  {
    //     let task_runs = self.storage
    //         .list_task_runs(0, 0, namespace_id, pipeline_id, run_id)
    //         .await?;
    // }
}
